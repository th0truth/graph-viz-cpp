#pragma once

#include <iostream>
#include <algorithm>
#include <chrono>
#include <climits>
#include <fstream>
#include <queue>
#include <set>
#include <string>
#include <vector>

using namespace std;

typedef int32_t i32;

#define TRACE(visualize, x) do { if (visualize) { cout << x; } } while(0)
